import { getBusiness } from "@/lib/business";
export default async function SettingsPage(){ const b=await getBusiness(); return <><div className="eyebrow">Business</div><h1>Settings</h1><div className="stack"><div className="field"><label>Business name</label><input value={b.name} readOnly/></div><div className="field"><label>Booking URL</label><input value={b.booking_url || "Not configured"} readOnly/></div><div className="field"><label>Default return interval</label><input value={`${b.default_interval_months} months`} readOnly/></div><p className="help">V1 keeps business settings read-only in the UI. They can be edited directly in the database until the first pilot workflow is proven.</p></div></> }
